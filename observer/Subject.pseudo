

//** CHANGE: interface to be implemented by button. will notify observer (observer.pseudo) of object state changes**//

interface Subject {
  	public void registerObserver(Observer Obs);
	public void unregisterObserver(Observer Obs);
	public void press(String newColor);

}
